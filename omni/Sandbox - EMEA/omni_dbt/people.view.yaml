# Reference this view as omni_dbt__people
schema_label: ""

schema: omni_dbt
table_name: PEOPLE

dimensions:
  person:
    sql: '"PERSON"'

  territory:
    sql: '"TERRITORY"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: people
  target_schema: SUPERSTORE
  config:
    materialized: view
  code: |-
    {{ config(materialized='view') }}

    select *
    from {{ source('superstore', 'PEOPLE') }}
