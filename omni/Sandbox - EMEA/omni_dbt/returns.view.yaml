# Reference this view as omni_dbt__returns
schema_label: ""

schema: omni_dbt
table_name: RETURNS

dimensions:
  returns:
    sql: '"RETURNS"'

  order_id:
    sql: '"ORDER_ID"'
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: returns
  target_schema: SUPERSTORE
  config:
    materialized: view
  code: |-
    {{ config(materialized='view') }}

    select *
    from {{ source('superstore', 'RETURNS') }}
