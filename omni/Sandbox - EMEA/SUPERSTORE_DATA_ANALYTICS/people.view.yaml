# Reference this view as superstore_data_analytics__people
schema: SUPERSTORE_DATA_ANALYTICS
table_name: PEOPLE

dimensions:
  person:
    sql: '"PERSON"'

  territory:
    sql: '"TERRITORY"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: people
  target_schema: SUPERSTORE_DATA_ANALYTICS
  config:
    materialized: view
  code: |-
    {{ config(materialized='view') }}

    select *
    from {{ source('superstore', 'PEOPLE') }}
