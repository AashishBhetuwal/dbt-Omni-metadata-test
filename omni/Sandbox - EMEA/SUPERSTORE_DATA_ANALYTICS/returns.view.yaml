# Reference this view as superstore_data_analytics__returns
schema: SUPERSTORE_DATA_ANALYTICS
table_name: RETURNS

dimensions:
  returns:
    sql: '"RETURNS"'

  order_id:
    sql: '"ORDER_ID"'
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: returns
  target_schema: SUPERSTORE_DATA_ANALYTICS
  config:
    materialized: view
  code: |-
    {{ config(materialized='view') }}

    select *
    from {{ source('superstore', 'RETURNS') }}
